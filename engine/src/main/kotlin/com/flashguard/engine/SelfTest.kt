package com.flashguard.engine

/**
 * Placeholder entry point so CI can validate the engine module wiring end-to-end.
 * Replaced by the real self-test corpus as the engine lands.
 */
fun main() {
    println("FlashGuard engine scaffold OK (placeholder self-test)")
}
